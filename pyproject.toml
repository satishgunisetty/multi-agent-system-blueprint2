[project]
name = "multi-agent-system-blueprint2"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "databricks-langchain>=0.11.0",
    "databricks-sql-connector>=4.2.1",
    "langchain-core>=1.1.0",
    "langgraph>=1.0.3",
    "langgraph-checkpoint-postgres>=3.0.1",
    "mlflow[databricks]>=3.6.0",
    "psycopg[binary,pool]>=3.2.13",
    "pydantic>=2.12.4",
]

[dependency-groups]
dev = [
    "black>=25.11.0",
]
